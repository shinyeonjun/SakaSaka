name: intent-world-ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  contracts-and-runtime:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run build:api
      - run: npm test -- --run
      - run: npm run build
      - run: npm run acceptance:desktop
      - run: npx playwright install --with-deps chromium
      - run: npm run acceptance
      - run: npm run acceptance:api
      - run: npm run acceptance:autonomous
      - run: npm run acceptance:experiments
      - run: npm run acceptance:ui

  security-and-gates:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run security:check
      - run: npm test -- --run src/security.test.ts
      - run: git diff --check
